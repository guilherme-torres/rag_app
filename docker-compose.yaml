services:
  app:
    build: .
    container_name: rag_app
    networks:
      - ollama-network
    env_file: .env
    ports:
      - "8080:8080"

networks:
  ollama-network:
    name: ollama_ollama-network
    external: true